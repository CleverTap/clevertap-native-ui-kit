# Expo apps frequently pull in legacy peer dep constraints from older RN
# community packages (e.g. react-native-fast-image still pinning React ^18).
# Auto-applying --legacy-peer-deps keeps `npm install` from blocking on those
# transitive conflicts. The same flag is used by Expo's own CLI templates.
legacy-peer-deps=true
