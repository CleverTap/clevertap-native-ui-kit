// Stub returned by metro.config.js when an optional UI peer dep is not
// installed. The SDK's `optionalDeps.ts` checks `=== null` and falls back to
// its degraded path (e.g. solid color instead of linear gradient).
module.exports = null;
