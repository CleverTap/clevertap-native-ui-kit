# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Auto-generated API reference output — regenerated by CI on every deploy.
# Local devs: run `./gradlew :sdk:dokkaHtml` and `xcodebuild docbuild` to
# populate locally; see website/README.md.
/static/api/android/*/
/static/api/ios/*/
# But keep the platform-level redirect index.html files committed:
!/static/api/android/index.html
!/static/api/ios/index.html

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
